let initState = {
    friends: [
      { id: 1, name: 'Slava' },
      { id: 2, name: 'Mark' },
      { id: 3, name: 'Gena' },
    ]
  }

const sitebarReducer = (state = initState, action)=> {
    
    return state;
}


export default sitebarReducer;